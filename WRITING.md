# WRITING.md（skill 写作规则）

新增或修改 `huohou-*` skill 时遵守。核心判断：**skill 的每一行都在花模型的注意力，花在废话上的每一分都是负资产。**

## 1. 废话测试

一句话只有能改变模型的默认行为才配存在。"要仔细""要全面""注意质量"这类正确但无用的句子，整句删掉——不是精简措辞，是删。判断不了是不是废话时，假设是，删。

## 2. 给目标，不给脚本

写清"什么算完成、完成拿什么证明"，路径选择留给模型。skill 是判断框架，不是逐字操作手册；步骤只写到"不可替代的判断点"为止，再往细写是花钱买僵化。

## 3. 清单只收敛，不堆积

加新条款前先问：它是不是某条既有原则的实例？是 → 折叠进那条，不新增；不是 → 才配独立成行。近义条款平铺三条，读的时候一条都记不住。

## 4. 规则不带来历

规则文本只说"防什么、怎么做"，不写"来自某次踩坑/某篇文章"。来历放 commit message；想留档，在该 skill 末尾加一行踩坑记录（发生了什么 → 定了什么规则）。规则本身保持无出处。

## 5. description 写给路由器，不写给人

description 决定触发率：穷举触发场景，含口语说法（"看看代码""收尾""又挂了"），再补一句"不管 X"划清与相邻 skill 的边界。不写功能说明书——正文才是说明书。

## 6. 载荷放对层

- 跨所有场景的行为约束 → 全局 `~/AGENTS.md`
- 特定场景触发的流程/判断 → skill
- 纯知识、不改变行为 → 笔记，不为知识做 skill（见 `huohou-digest` 第 5 步）

## 7. 改完验收

skill 改动提交前，用 `huohou-code-review` 的纪律过一遍自己的 diff；改了 description 或触发语义时，同步检查 README 收录表。
