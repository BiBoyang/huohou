# Session 2026-09-17

## 今日完成

- 引入阿里 skill-up（Agent Skill 评测工具），本地 build 出 `~/skill-up/bin/skill-up`，并为其编写了 kimi custom engine 适配器 `evals-shared/kimi_engine.py`（SessionInput ↔ `kimi -p` stream-json；`--skills-dir` 固定实现 with/without 真隔离；`KIMI_CODE_HOME` 隔离用户 hooks）
- 为全部 9 个 skill 建立 `evals/`（声明式 YAML 用例 + agent_judge/rule_based/expect），全部跑出首轮报告（各 `huohou-*-workspace/`，已 gitignore）
- huohou-polish 完成一轮完整演进闭环：评测发现「不管全文翻译」红线失守 → SKILL.md 红线节新增「守边界」条款 → 回归验证 with_skill 100% PASS
- 给 alibaba/skill-up 提 issue #253：skill 源为符号链接时 `filepath.Walk` 不跟随链接根，静默安装 0 文件导致 benchmark 失效（本仓库 skills 正是符号链接布局，已用真实路径绕过）
- 清理评测副作用：rust-expert 用例的 agent 在宿主机留下 crontab 清理任务和 `~/bin/cleanup_tmp.py`，已移除

## 关键决策

- 评测引擎选 kimi custom（local transport）而非 claude/codex：agent_judge 复用同一 custom engine，无需额外模型凭据
- 首轮用例只做单轮：custom engine 多轮只支持 batch 拼接
- `environment: none` + `parallelism: 1`：换速度，但代价是被测 agent 有宿主机完整权限（副作用事件的教训）

## 遗留问题

- 跨 skill 共性发现：「不管」类边界声明约束力普遍弱——code-review（1/2 失效）、rust-expert（实测无效）、wrap-up（用户授权覆盖闸门）、swift-concurrency（施压下软化）；polish、plan-first 已验证守住。各 SKILL.md 的加固待逐个拍板
- 单次运行噪声大（no-inflation 两轮结果相反），后续结论性对比用 `skill-up run --iteration N` 采样
- recover-from-errors 的 drift 用例素材与口述不一致（用例设计问题），待下轮迭代

## 备注

- skill-up 两个使用要点：`skills[].path` 必须用符号链接解析后的真实路径；报告目录 `huohou-*-workspace/` 已加入 .gitignore
