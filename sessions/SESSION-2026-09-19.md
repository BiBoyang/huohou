# Session 2026-09-19

## 今日完成

- **第四波：description 演化闭环（工具管理自家 skill 的第一次演化）**：
  - plan-first 补代码锚定例句：kimi R 0.833→1.000（FN 消除），polish 边界无损
  - wrap-up 收窄排除条款：**claude P 0.857→1.000（FP 消除），kimi FP 仍在**——「不管」条款约束力是引擎相关属性（kimi 被触发词正吸引主导，负约束低敏感），为 09-17 遗留的共性问题提供定量实证
  - 上游 issue 证据内联：Issue 2 现场复现拿 mtime 实锤（"1 passed" 但产物零更新）
  - 补强：trigger_report 排除计数（EXCLUDED 行，分母缩水不再静默）+ selftest 配置不变量断言（25 条）
- **第三波：9-skill 触发评测全量（n=90，kimi）+ 外部 review 修正**：
  - 7 个 skill 补齐触发用例（各 10 条含 near-miss）；宏平均 P=0.9841 R=0.9815 F1=0.9814，总矩阵 TP=51 FP=1 FN=1 TN=37
  - 口径统一（外部 review 抓出）：协议激活 = Skill 工具调用；Read SKILL.md = 影子使用信号（独立呈现，不进矩阵不减 recall）；超时丢数据不进分母且**显式报排除计数**
  - collab-mode 协议内容询问按契约（AGENTS.md 第 6 条）改判负样本；claude 复核：plan-first 模糊口语 kimi/claude 分歧（引擎敏感性），collab-mode Read 路径双引擎复现
  - 用例设计 4 处瑕疵修正（重任务 prompt action-free 化、类比语境重标负样本、cron 诱导换无害闲聊），升格为纪律条款进 memo
  - 上游协同：`evals-shared/upstream-issues.md` 两篇 GitHub-ready 草稿（超时强杀丢产物、增量重跑不落盘），open issues 已核无重叠
  - selftest 增至 25 断言：+口径 rubric 3 条（Read 不算激活/亮影子/不减 recall）、+claude 配置不变量 5 条（首用即抓到改判残留旧 case 名）；trigger_report 支持**排除计数**与多目录聚合（同 case 取最新）
- 两周狗粮验证 Day 1-2 完成核心使命（立项依据达成），三条验证线全部实弹落地：
  - **lint**：`evals-shared/lint_skill.py`（frontmatter/name/description≤1024/引用文件存在/密钥扫描）+ GitHub Actions（`.github/workflows/lint.yml`），9 skill 基线全绿、坏样本 4 类全抓
  - **触发评测**：`kimi_engine.py` 升级 trace 保真（tool_call/tool_result 结构化，向后兼容）；code-review + wrap-up 各 10 条触发用例（6 正 4 负含 near-miss）；`evals-shared/trigger_report.py` 离线判活算 P/R/F1
  - **flaky/delta**：`evals-shared/flaky.py`（Flaky Rate 与 Error Rate 分账）、`evals-shared/delta.py`（skill lift + 延迟增量 + token 增量降级代理）
- **双引擎**：新增 `evals-shared/claude_engine.py`（per-case 隔离 CLAUDE_CONFIG_DIR、env 凭据剥离拷贝、try/finally 清理）；20 条触发用例 × kimi/claude 双引擎跑完，判活工具零改动——"runner 无关"主张 2/2 兑现
- **评测器自测**：`evals-shared/fixtures/bad-skill/` + `evals-shared/selftest.py`（15 断言进 CI），首日抓到判活器对字符串形态 arguments 的崩溃 bug
- 埋雷验证：删 wrap-up 触发词 → Recall 1.0→0.333 被 trigger_report 逐条抓获，同一时刻 skill-up 报 "6 passed" 零感知；SKILL.md 已还原（diff 验证干净）
- 立项 memo 定稿：`skill-eval-handoff/MEMO.md`（双路证据 + 双引擎数据 + 预算画像）

## 关键决策

- 触发评测不走 judge：skill-up 只当"发 prompt + 落 transcript"的 runner，激活判定离线可复算（省一半模型调用）
- `benchmark.enabled: false` 用于触发评测（with_skill 单臂，激活判定不需要 without 对照）
- flaky 分账：Flaky Rate 只算 PASS/FAIL 摇摆，ERROR 单列 Error Rate（预算截断事件实证：claude --max-turns 6 使激活型用例整批 ERROR，15 够用）
- 立项三决策（用户拍板）：新开独立仓库、Python 起步、Codex 为下一个引擎

## 关键发现

- kimi 与 claude 的 Skill 工具调用事件天然同构（tool_call.name=="Skill" + arguments.skill），判活走一等公民路径，无需 hack
- wrap-up 触发面缺陷跨引擎逐 case 复现：「写 commit message 不提交」FP 在 kimi/claude 上同现（P=0.857/R=1.0），排除了引擎噪声解释；description 收窄待 owner 拍板
- claude 有真 token usage（modelUsage），kimi stream-json 无——跨引擎 usage 规范化成为适配层独立卖点
- 触发评测成本画像：未触发负样本 ~54 tokens/1.7s，正样本 ~7K tokens/~40s，负样本扩容近乎免费

## 遗留问题

- wrap-up 的 kimi 侧残留 FP：「独立写 commit message 不提交」在 kimi 上仍误触发（claude 已消除）——排除条款对 kimi 路由约束力弱，接受为引擎差异或第三轮加码措辞待拍板
- 「不管」类边界声明约束力弱的共性问题：本轮拿到定量实证（同一 description 修改双引擎结果分化），根治可能需要引擎侧机制，description 工程有天花板
- 多轮隐式触发（`o` 确认链）未覆盖
- claude 引擎 DeepSeek 端点映射依赖用户 settings env，换机器需确认
- 上游进展：#263 已有 PR #265（超时合成 session-result）；#264 后确认为我方误报（重跑行为正确，观测错误源于相对路径 output-dir 与 stat 错 iteration），更正评论待发；适配器加 SIGTERM guard 双保险（selftest 32 断言）

## 备注

- claude 触发评测配置：`evals/eval-triggers-claude.yaml`（与 kimi 版仅差引擎名与适配器路径）
- 实弹产物目录 `huohou-*-triggers-*-workspace/` 均被既有 gitignore 模式覆盖；凭据安全已核（全文件密钥扫描 + diff 亲验 + 无 .claude-eval-home 残留）
